name: velox_animations_example
description: Example app demonstrating velox_animations.
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  velox_animations:
    path: ../
